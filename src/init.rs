pub fn init() {
    dbg!("init function has run.");


    // create a .toml file with user input.
    // parse the toml file with config crate to use its values.
    // according to those values find the location of db.pmanager
}
